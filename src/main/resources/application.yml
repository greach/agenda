micronaut:
    application:
        name: agenda
    router:
        static-resources:
            default:
                enabled: true
                mapping: /**
                paths:
                    - classpath:static